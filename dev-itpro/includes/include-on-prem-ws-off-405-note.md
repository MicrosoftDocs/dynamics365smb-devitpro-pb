> [!NOTE]
> For on-premises environments where SOAP, OData, or APIs have been turned off, any call to such an endpoint will also return an HTTP status code 405 (so check your server configurations if you see those). 
